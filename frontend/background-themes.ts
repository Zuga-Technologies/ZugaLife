// Re-export shim — canonical source moved to @core/wallpaper/registry.
// Preserved here so existing imports (`./background-themes`) keep working.
// New code should import from '@core/wallpaper' instead.
export * from '@core/wallpaper/registry'
